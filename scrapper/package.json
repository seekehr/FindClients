{
  "name": "findclients-scrapper",
  "version": "0.1.0",
  "private": true,
  "description": "Platform scrapers for FindClients. Loaded at runtime by ../server.",
  "scripts": {
    "cli": "tsx cli.ts"
  },
  "dependencies": {
    "playwright": "^1.62.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
