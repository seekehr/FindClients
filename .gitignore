# Your data — leads, config, saved sessions. Never commit this.
data/

# Environment — everything except the committed template.
# Broad on purpose: .env.backup, .env.old and friends hold real keys too.
.env*
!.env.example

# Dependencies and build output
node_modules
.next/
dist/

# Editor / OS
.DS_Store
.claude/
.cursor/
*.log
.idea/

# Build metadata
*.tsbuildinfo
